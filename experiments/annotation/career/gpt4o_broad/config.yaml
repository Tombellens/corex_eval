task: annotation
variable: career_position
model: gpt-4o
contributor: tom
description: >
  GPT-4o (OpenAI) — zero/few-shot prompting for career_position annotation
  (broad sector codes).
notes: >
  N_SHOTS=0 (zero-shot) by default; set to 3 or 5 for few-shot.
  Standardised prompt template; temperature=0; max_tokens=64.
  OpenAI API (requires OPENAI_API_KEY).
